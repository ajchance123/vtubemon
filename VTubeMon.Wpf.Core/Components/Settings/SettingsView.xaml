<UserControl x:Class="VTubeMon.Wpf.Core.Components.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VTubeMon.Wpf.Core.Components.Settings"
             DataContext="{Binding SettingsViewModel, Source={StaticResource Locator}}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ComboBox
            SelectedItem="{Binding SelectedSkin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ItemsSource="{Binding SkinCollection}"
            VerticalAlignment="Top"
            HorizontalAlignment="Left">
        </ComboBox>
    </Grid>
</UserControl>
